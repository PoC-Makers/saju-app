# 사주 계산 코어 조사 — 간지·진태양시·절기·음력

`packages/core`의 `calendar/`·`pillars/` 설계를 확정하기 위한 사전 조사.
대상은 네 가지: ① 60갑자 순환(일주) ② 진태양시 보정·자시 경계 ③ 절기 기반 월주·입춘 기준 년주 ④ 음력 입력 지원.

> **결론 먼저**
> - 표준시 이력·서머타임은 **직접 구현하지 않는다.** IANA tz DB(`Asia/Seoul`)가 전부 갖고 있음을 실측 확인했다.
> - 진태양시는 "KST − 32분"이 아니라 **UTC 절대시각 + 실제 경도 + 균시차**로 계산한다. 표준자오선이 시대별로 바뀌었기 때문.
> - 절기는 천문 계산(태양황경)이 필요하다 → `astronomy-engine`.
> - 음력은 KASI 데이터 기반 라이브러리를 쓴다. 중국 농력과 윤달이 다르므로 중국계 라이브러리 금지.
> - **일주 기준일(epoch)은 확정됐다.** JDN에서 직접 유도하며 별도 상수가 없다. 55,152일 전수 검증 완료. (→ §1.2)

---

## 1. 60갑자 순환과 일주

### 1.1 구조

천간 10 × 지지 12 → 최소공배수 60주기. 짝수-짝수/홀수-홀수만 결합하므로 120이 아닌 60이다.

```
천간(天干) 갑을병정무기경신임계          index % 10
지지(地支) 자축인묘진사오미신유술해       index % 12
간지(干支) index 0 = 갑자 … 59 = 계해
```

년·월·시주는 규칙으로 유도되지만(→ §3), **일주만은 유도 규칙이 없다.** 끊김 없이 흘러온 순환이라 기준일로부터의 경과일수로만 구한다.

```
일주 index = (해당일의 JDN − 기준일 JDN) mod 60
```

### 1.2 ✅ 기준일(epoch) 확정 — 전수 검증 완료 (2026-09-13)

**결론: 별도 epoch 상수가 필요 없다.** JDN에서 직접 유도하는 표준식을 쓴다.

```ts
// 천간·지지를 JDN에서 직접 유도 — 기준일 상수 불필요
const stemIndex   = (jdn + 9) % 10;   // 0=갑
const branchIndex = (jdn + 1) % 12;   // 0=자
```

**검증 경위**

처음에 "1984-02-02 = 갑자일"과 "1900-01-01 = 갑술일"이 2일 어긋나 보였으나, **전자가 착오였다.**
1984년은 갑자**년**(60년 주기의 시작)이고 갑자**일**이 아니다. 실제 1984-02-02는 **병인일**이다.
년주 앵커와 일주 앵커를 혼동한 것으로, 사주 자료에서 흔한 함정이다.

**전수 대조 결과**

| 항목 | 값 |
|------|-----|
| 방법 A | JDN 순수 산술 (위 식) |
| 방법 B | `korean-lunar-calendar` (KASI 천문데이터 기반) `getKoreanGapja()` |
| 대조 범위 | 1900-01-01 ~ 2050-12-31 |
| 대조 일수 | **55,152일** |
| 불일치 | **0일** |

서로 독립적인 두 방법(순수 산술 vs KASI 데이터 테이블)이 150년 전 기간에서 완전히 일치한다.
표준시 전환일(1954-03-21, 1961-08-10)과 서머타임 구간(1955~1960, 1987~1988)도 포함된 범위다.

> **일주는 표준시·서머타임과 무관하다.** 달력상 날짜만으로 결정되므로 §2의 시간 보정이 일주를 바꾸지 않는다.
> 단 **자시 경계 옵션(§2.4)은 예외** — 그건 "날짜를 하루 넘길 것인가"의 문제라 일주에 직접 영향을 준다.

**남은 확인 (선택)**

방법 B가 KASI 데이터 기반이긴 하나 KASI 원본은 아니다. 원한다면 [공공데이터포털 KASI 음양력 API](https://www.data.go.kr/data/15012679/openapi.do)로 최종 확인할 수 있다(`lunIljin` 필드). **서비스키 발급 필요.** 지원 범위는 1391-02-05 ~ 2050-12-31로, 이 API는 율리우스적일(JDN)도 함께 반환하므로 위 식을 직접 대조하기 좋다.
다만 독립 2개 방법이 55,152일 전수 일치했으므로 **구현을 막는 요소는 아니다.**

> 교훈: 사주 계산에서 "널리 알려진 상수"는 검증 없이 쓰면 안 된다. 다만 이번 건은 자료가 틀린 게 아니라 **년주 앵커를 일주 앵커로 잘못 읽은** 경우였다.

### 1.3 구현 메모

- 날짜 → JDN 변환은 정수 연산으로. `Date` 객체의 로컬 타임존 개입을 차단하기 위해 **UTC 기준 일수**로만 다룬다.
- `Math.floor` vs `Math.round` — 부동소수점 오차로 하루가 밀리는 사고가 잦다. 정수 나눗셈으로 구현하고 경계일(자정 전후) 테스트를 반드시 넣는다.
- JS의 `%`는 음수에서 음수를 반환한다. `((n % 60) + 60) % 60` 형태를 강제한다.

---

## 2. 진태양시 보정과 자시 경계

### 2.1 왜 "KST − 32분"이 틀린가

흔한 구현은 서울 경도(126.978°E)와 표준자오선(135°E) 차이 8.022° × 4분 = **약 32분**을 일괄로 뺀다. 두 가지가 빠져 있다.

**(a) 표준자오선이 시대별로 바뀌었다.** 32분은 현재(135°)에만 맞는 값이다.

**(b) 균시차(Equation of Time)가 빠졌다.** 지구 공전궤도의 이심률과 자전축 기울기 때문에 진태양시는 평균태양시 대비 **−14분 ~ +16분** 진동한다. 연중 최대 30분 폭이므로 시주(2시간 단위)의 경계를 넘길 수 있다.

### 2.2 한국 표준시 이력 (실측 검증 완료)

Node 22의 `Intl` (IANA tz DB) 로 `Asia/Seoul`의 과거 오프셋을 직접 조회한 결과:

| 날짜 | tz DB 오프셋 | 해석 |
|------|-------------|------|
| 1910-06-01 | **+08:30** | 대한제국 표준시 (127.5°) |
| 1930-06-01 | +09:00 | 일제강점기, 일본 표준시 (135°) |
| 1958-06-01 | **+09:30** | 8:30 표준시 **+ 서머타임 1h** |
| 1960-06-01 | **+09:30** | 〃 |
| 1962-06-01 | +09:00 | 1961-08-10 이후 135° 복귀 |
| 1987-08-01 | **+10:00** | 9:00 + 서머타임 1h (서울올림픽 대비) |
| 1988-08-01 | **+10:00** | 〃 |
| 1990-08-01 | +09:00 | 서머타임 폐지 |
| 2026-09-13 | +09:00 | 현재 |

주요 전환점: 1954-03-21 → UTC+08:30(127.5°) / 1961-08-10 → UTC+09:00(135°) 복귀.
서머타임 시행기: **1948~1951, 1955~1960, 1987~1988.**

> **→ tz DB가 표준시 변경과 서머타임을 모두 정확히 담고 있다. 직접 테이블을 만들지 않는다.**
> 손으로 만든 표는 반드시 어딘가 틀리고, tzdata는 전 세계가 검증한다.

### 2.3 올바른 계산 파이프라인

```
입력: 생년월일시(로컬 벽시계) + 출생지 경도
  │
  ├─ ① 벽시계 → UTC 절대시각      ... tz DB (Asia/Seoul). 표준시 변경·서머타임 자동 처리
  │
  ├─ ② UTC → 평균태양시           ... UTC + (경도 ÷ 15)시간
  │
  ├─ ③ 평균태양시 → 진태양시       ... + 균시차(EoT)
  │
  └─ ④ 진태양시 → 시지(時支)       ... 2시간 단위 구간
```

**핵심: ②가 경도를 직접 쓰므로 표준자오선이 몇 도였는지 알 필요가 없다.** 시대별 분기 없이 한 식으로 모든 연도가 처리된다. 1958년생이든 2026년생이든 같은 코드가 맞는 답을 낸다.

- 균시차는 `astronomy-engine`으로 구한다(→ §3.3). 별도 근사식(Spencer 등)을 쓰면 정밀도 관리 부담만 생긴다.
- 출생지 경도는 **입력받아야 한다.** 서울 고정은 제주(126.5°)·독도(131.9°) 기준 최대 20분 이상 오차 → 시주가 바뀔 수 있다. Phase 0에서는 서울 기본값 + 주요 도시 선택으로 시작하고, 좌표 입력은 뒤로 미룬다.

### 2.4 자시 경계 — 유파 선택 문제

子시는 23:00~01:00으로 **하루를 걸친다.** 여기서 23:00~24:00(야자시)의 일주를 어떻게 볼지 학설이 갈린다.

| 유파 | 23:00~24:00생의 일주 | 시주 |
|------|---------------------|------|
| **야자시(夜子時) 구분** | 당일 일주 유지 | 자시 |
| **조자시/일괄 변경** | 다음날 일주로 넘김 | 자시 |

**정답이 없다. 유파의 선택이다.** 따라서:

- 코어는 **옵션으로 노출**한다: `{ nightZiHour: "keep-day" | "next-day" }`
- 기본값을 정하고 그 근거를 문서에 남긴다 (국내 상용 서비스 다수는 다음날로 넘기는 쪽).
- 결과 화면에 "어떤 기준으로 계산했는지" 표기한다 → 신뢰 장치. (사주도령 기능맵의 "신뢰 장치" 항목과 연결)

> 이 경계는 **진태양시 보정 이후**에 판정해야 한다. 순서를 뒤집으면 32분 보정 때문에 23:00 전후 출생자가 통째로 틀린다.

---

## 3. 절기 기반 월주 · 입춘 기준 년주

### 3.1 사주의 한 해는 1월 1일에 시작하지 않는다

년주는 **입춘(立春)** 을 기점으로 바뀐다. 1월 1일도, 음력 설도 아니다.

- 2026년 2월 3일생 → 아직 **2025년(을사)** 의 년주
- 2026년 2월 5일생 → **2026년(병오)** 의 년주

입춘은 매년 2월 3~5일 사이에서 움직이므로 **날짜 하드코딩이 불가능하다.** 반드시 계산해야 한다.

### 3.2 12절(節)이 월의 경계다

24절기 중 **절(節)** 12개만 월 경계로 쓴다. (중기(中氣) 12개는 쓰지 않는다.)

| 절기 | 태양황경 | 시작되는 월지 |
|------|---------|--------------|
| 입춘 | 315° | 寅(인) |
| 경칩 | 345° | 卯(묘) |
| 청명 | 15° | 辰(진) |
| 입하 | 45° | 巳(사) |
| 망종 | 75° | 午(오) |
| 소서 | 105° | 未(미) |
| 입추 | 135° | 申(신) |
| 백로 | 165° | 酉(유) |
| 한로 | 195° | 戌(술) |
| 입동 | 225° | 亥(해) |
| 대설 | 255° | 子(자) |
| 소한 | 285° | 丑(축) |

월지가 寅부터 시작하는 것이 **인월세수(寅月歲首)** — 년주 교체점이 입춘인 이유와 같은 뿌리다.

### 3.3 절기 계산 — `astronomy-engine`

절기 = 태양의 겉보기 황경이 15°의 배수에 도달하는 순간. 천문 계산이 필수다.

- **`astronomy-engine`** (npm, MIT, cosinekitty)
  - `SearchSunLongitude(targetLon, startTime, limitDays)` — 임의 황경 도달 시각을 직접 검색. 절기 계산에 그대로 맞는 API.
  - VSOP87 / NOVAS C 3.1 기반, 정밀도 **±1 arcmin**. 절기 시각 오차는 분 단위 이내로 충분하다.
  - 의존성 없는 순수 JS/TS → `packages/core`의 "프레임워크 독립" 원칙에 부합.
  - 균시차(§2.3 ③)도 같은 라이브러리로 해결 → 의존성 하나로 절기 + EoT 동시 커버. **채택 권고.**
- `suncalc`는 가볍지만 황경 정밀 검색 API가 없다. 부적합.
- 절기 시각은 계산 비용이 있으므로 **연 단위로 캐시**한다 (한 해 12개 값).

### 3.4 월간(月干)·시간(時干)은 규칙으로 유도

지지가 정해지면 천간은 표로 결정된다. 계산이 아니라 매핑이다.

**월간 — 오호둔(五虎遁): 년간 → 인월의 천간**

| 년간 | 인(寅)월의 천간 |
|------|----------------|
| 갑·기 | 병인 |
| 을·경 | 무인 |
| 병·신 | 경인 |
| 정·임 | 임인 |
| 무·계 | 갑인 |

**시간 — 오서둔(五鼠遁): 일간 → 자시의 천간**

| 일간 | 자(子)시의 천간 |
|------|----------------|
| 갑·기 | 갑자 |
| 을·경 | 병자 |
| 병·신 | 무자 |
| 정·임 | 경자 |
| 무·계 | 임자 |

> 시간이 **일간**에서 유도된다는 점이 중요하다. §2.4의 야자시 선택이 일주를 바꾸면 **시주까지 연쇄로 바뀐다.** 두 옵션은 독립이 아니다 — 테스트에서 반드시 함께 검증한다.

---

## 4. 음력 입력 지원

### 4.1 왜 필요한가

어르신 세대는 생일을 음력으로 기억하는 경우가 많다. 음력 입력은 **국내 사주 서비스의 사실상 필수 기능**이다.

### 4.2 한국 음력 ≠ 중국 농력

둘 다 삭망월 기반이지만 **기준 경도가 다르다.** 한국은 KST 기준으로 삭(朔)과 절기 시각을 판정하므로, 경계에 걸리면 **날짜가 하루 어긋나거나 윤달 위치가 달라진다.**

> **중국계 라이브러리(`lunar-javascript` 등)를 쓰면 안 된다.** 사주 계산 기능이 내장되어 편해 보이지만 중국 시간 기준이라 한국 사주와 결과가 어긋난다.

### 4.3 후보 라이브러리 (KASI 기반)

| 라이브러리 | 지원 범위 | 비고 |
|-----------|----------|------|
| `korean_lunar_calendar_js` (usingsky) | 음력 1000-01-01 ~ 2050-11-18 | 범위가 가장 넓음 |
| `korean-lunar-calendar` (jangjunha) | — | KASI 천문 데이터 기반 |
| `kor-lunar-js` (kahyou22) | 1890-01-21 ~ 2050-12-31 | `isLeapMonth` 옵션 명시 |
| `manseryeok` (yhj1024, TS) | 1391 ~ 2100 | TS 네이티브, 사주 기능 포함, 한·중 윤달 차이 보정 명시 |

**선정 기준**: ① 윤달 플래그 지원 ② 우리가 필요한 연도 범위(최소 1900~2050) ③ TS 타입 ④ 유지보수 상태 ⑤ 라이선스.
→ 후보 2~3개로 좁혀 **동일 입력에 대한 출력 대조 테스트**를 먼저 돌린다. 서로 다르면 KASI 원본이 정답.

### 4.4 윤달(閏月) 처리

음력 입력은 `(연, 월, 일)` 3개로 부족하다. **윤달 여부 플래그가 반드시 있어야 한다.**

```ts
type LunarDate = { year: number; month: number; day: number; isLeapMonth: boolean };
```

- UI에서 해당 연도에 윤달이 없으면 체크박스를 비활성화한다.
- 음력은 **입력 형식일 뿐**이다. 받는 즉시 양력으로 변환하고, 코어 내부는 양력 하나로만 흐른다. 음력이 파이프라인 안쪽까지 들어오면 복잡도가 폭발한다.

---

## 5. 종합 — 계산 파이프라인과 폴더 매핑

```
[입력] 생년월일시 · 양/음력 · 출생지 경도 · 성별
   │
   ├─ (음력이면) 양력 변환                    calendar/lunar.ts
   ├─ 벽시계 → UTC (tz DB)                    calendar/timezone.ts
   ├─ UTC + 경도 + 균시차 → 진태양시            calendar/true-solar-time.ts
   ├─ 절기 시각 계산 (태양황경)                 calendar/solar-terms.ts
   │
   ├─ 년주 ← 입춘 기준                         pillars/year-pillar.ts
   ├─ 월주 ← 12절 + 오호둔                     pillars/month-pillar.ts
   ├─ 일주 ← JDN mod 60 + 자시 옵션            pillars/day-pillar.ts
   └─ 시주 ← 진태양시 + 오서둔(일간 의존)        pillars/hour-pillar.ts
```

`docs/conventions/filesystem.md`의 `packages/core` 구조와 그대로 맞는다. 새 폴더는 필요 없다.

### 의존성 추가 예정

| 패키지 | 용도 | 상태 |
|--------|------|------|
| `astronomy-engine` | 절기(태양황경) + 균시차 | 채택 권고 |
| 음력 변환 (§4.3에서 선정) | 음↔양 변환 | **미확정** — 대조 테스트 후 결정 |

tz DB는 **런타임 내장**이라 의존성 추가가 없다. 단, Node 빌드에 full-icu가 포함되어야 한다(Node 13+ 기본 포함).

---

## 6. 구현 전 결정해야 할 것

> **결정 내역의 단일 출처는 [결정사항 문서](2026-09-13-saju-core-decisions.md)다.** 아래는 현황 요약이며, 근거와 파급은 그쪽에 있다.

| # | 결정 사항 | 상태 |
|---|----------|------|
| D1 | 일주 epoch | ✅ **JDN 직접 유도** — §1.2, 55,152일 전수 검증 |
| D2 | 자시 경계 | ✅ **`keep-day`** (야자시 구분) — §2.4 |
| D3 | 음력 라이브러리 | ⬜ 미정 — §4.3. `korean-lunar-calendar`가 일진 검증에서 신뢰성 확인됨 |
| D4 | 지원 연도 범위 | ⬜ 미정 — 1900~2050 제안 (KASI 데이터 상한이 2050) |
| D5 | 출생지 입력 | ✅ **서울 고정** — 단 코어 API는 경도를 인자로 받는다 |
| D6 | 시각 미상 | ✅ **3주 모드** — `hour: Pillar \| null` |

**구현 차단 항목은 모두 해소됐다.** D3·D4는 구현과 병행 가능.

## 7. 검증 전략

사주 계산은 **틀려도 화면에 그럴듯하게 나온다.** 눈으로는 검증이 안 되므로 외부 정답지가 필수다.

1. **KASI 만세력을 정답지로** — 일진·절기·음양력 변환을 직접 대조.
2. **경계값 위주 테스트** — 일반 케이스는 대충 짜도 맞는다. 틀리는 건 항상 경계다.
   - 입춘 전후 (년주 교체)
   - 각 절기 전후 (월주 교체)
   - 23:00~01:00 (자시·일주 교체)
   - 진태양시 보정으로 시주가 넘어가는 케이스
   - 서머타임 시행기(1955~1960, 1987~1988) 출생
   - 표준시 전환일(1954-03-21, 1961-08-10) 전후
   - 윤달 생일
3. 위 케이스를 **KASI 값과 함께 픽스처로 고정**한다. 리팩터링 시 회귀 방지.

---

## 참고

- [Time in South Korea — Wikipedia](https://en.wikipedia.org/wiki/Time_in_South_Korea)
- [UTC+08:30 — Wikipedia](https://en.wikipedia.org/wiki/UTC+08:30)
- [South Korea time changes — timeanddate.com](https://www.timeanddate.com/time/change/south-korea?year=1961)
- [Astronomy Engine — GitHub](https://github.com/cosinekitty/astronomy)
- [astronomy-engine — npm](https://www.npmjs.com/package/astronomy-engine)
- [korean_lunar_calendar_js — GitHub](https://github.com/usingsky/korean_lunar_calendar_js)
- [korean-lunar-calendar — GitHub](https://github.com/jangjunha/korean-lunar-calendar)
- [kor-lunar-js — GitHub](https://github.com/kahyou22/kor-lunar-js)
- [manseryeok (TS) — GitHub](https://github.com/yhj1024/manseryeok)

- [한국천문연구원_음양력 정보 — 공공데이터포털](https://www.data.go.kr/data/15012679/openapi.do) (서비스키 필요, 1391~2050, `lunIljin`·JDN 반환)
- [KASI 천문우주지식정보 Open API](https://astro.kasi.re.kr/information/pageView/31)

---

*검증 기록 (2026-09-13, Node 22.12.0)*
- *§2.2 표준시 오프셋 표 — IANA tz DB(`Asia/Seoul`) 실측 조회.*
- *§1.2 일주 산식 — JDN 순수 산술 vs `korean-lunar-calendar`, 1900~2050 전 55,152일 전수 대조, 불일치 0.*
