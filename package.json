{
  "name": "saju-app",
  "private": true,
  "packageManager": "yarn@4.18.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "setup": "bash scripts/setup/bootstrap.sh"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.10.12"
  }
}
